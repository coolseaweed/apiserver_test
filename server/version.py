# set version info (for logging)
version_number = "0.0.1"
version_info = "initial version"

# set app info (for swagger)
app_title = f"FastAPI Starter API v{version_number}"
app_description = f"FastAPI Starter API v{version_number}"
